(function totalNota2(win, doc) {
    'use strict';

    quant1.addEventListener('keyup', function () {
        let multQuant1ePrecouni1 = parseInt(quant1.value) * parseInt(precoUni1.value) + parseInt(frete1.value);
        let multQuant1ePrecouni1s = multQuant1ePrecouni1.toFixed(2);

        if (quant1.value == '') {
            multQuant1ePrecouni1s = `...`;
        }
        let result1s = result.innerHTML = `R$${multQuant1ePrecouni1s} `;

        precoUni1.addEventListener('keyup', function () {
            let multQuant1ePrecouni1 = parseInt(quant1.value) * parseInt(precoUni1.value) + parseInt(frete1.value);
            let multQuant1ePrecouni1s = multQuant1ePrecouni1.toFixed(2);

            if (quant1.value == '') {
                multQuant1ePrecouni1s = `...`;
            }
            let result1s = result.innerHTML = `R$${multQuant1ePrecouni1s} `;
        }, false)

    }, false);

    /* ---------------FIM da function responsavel pela linha 1 ------------------------ */

    /* ----------------------------------------------- */

    quant2.addEventListener('keyup', function () {
        let multQuant2ePrecouni2 = parseInt(quant2.value) * parseInt(precoUni2.value) + parseInt(frete2.value);
        let multQuant2ePrecouni2s = multQuant2ePrecouni2.toFixed(2);

        if (quant2.value == '') {
            multQuant2ePrecouni2s = `...`;
        }
        let result2s = result2.innerHTML = `R$${multQuant2ePrecouni2s} `;

        precoUni2.addEventListener('keyup', function () {
            let multQuant2ePrecouni2 = parseInt(quant2.value) * parseInt(precoUni2.value) + parseInt(frete2.value);
            let multQuant2ePrecouni2s = multQuant2ePrecouni2.toFixed(2);

            if (quant2.value == '') {
                multQuant2ePrecouni2s = `...`;
            }
            let result2s = result2.innerHTML = `R$${multQuant2ePrecouni2s} `;
        }, false)

    }, false);

    /* ---------------FIM da function responsavel pela linha 2 ------------------------ */

    /* ----------------------------------------------- */

    quant3.addEventListener('keyup', function () {
        let multQuant3ePrecouni3 = parseInt(quant3.value) * parseInt(precoUni3.value) + parseInt(frete3.value);
        let multQuant3ePrecouni3s = multQuant3ePrecouni3.toFixed(2);

        if (quant3.value == '') {
            multQuant3ePrecouni3s = `...`;
        }
        let result3s = result3.innerHTML = `R$${multQuant3ePrecouni3s} `;

        precoUni3.addEventListener('keyup', function () {
            let multQuant3ePrecouni3 = parseInt(quant3.value) * parseInt(precoUni3.value) + parseInt(frete3.value);
            let multQuant3ePrecouni3s = multQuant3ePrecouni3.toFixed(2);

            if (quant3.value == '') {
                multQuant3ePrecouni3s = `...`;
            }
            let result3s = result3.innerHTML = `R$${multQuant3ePrecouni3s} `;
        }, false)

    }, false);

    /* ---------------FIM da function responsavel pela linha 3 ------------------------ */

    /* ----------------------------------------------- */

    quant4.addEventListener('keyup', function () {
        let multQuant4ePrecouni4 = parseInt(quant4.value) * parseInt(precoUni4.value) + parseInt(frete4.value);
        let multQuant4ePrecouni4s = multQuant4ePrecouni4.toFixed(2);

        if (quant4.value == '') {
            multQuant4ePrecouni4s = `...`;
        }
        let result4s = result4.innerHTML = `R$${multQuant4ePrecouni4s} `;

        precoUni4.addEventListener('keyup', function () {
            let multQuant4ePrecouni4 = parseInt(quant4.value) * parseInt(precoUni4.value) + parseInt(frete4.value);
            let multQuant4ePrecouni4s = multQuant4ePrecouni4.toFixed(2);

            if (quant4.value == '') {
                multQuant4ePrecouni4s = `...`;
            }
            let result4s = result4.innerHTML = `R$${multQuant4ePrecouni4s} `;
        }, false)

    }, false);

    /* ---------------FIM da function responsavel pela linha 4 ------------------------ */

    /* ----------------------------------------------- */

    quant5.addEventListener('keyup', function () {
        let multQuant5ePrecouni5 = parseInt(quant5.value) * parseInt(precoUni5.value) + parseInt(frete5.value);
        let multQuant5ePrecouni5s = multQuant5ePrecouni5.toFixed(2);

        if (quant5.value == '') {
            multQuant5ePrecouni5s = `...`;
        }
        let result5s = result5.innerHTML = `R$${multQuant5ePrecouni5s} `;

        precoUni5.addEventListener('keyup', function () {
            let multQuant5ePrecouni5 = parseInt(quant5.value) * parseInt(precoUni5.value) + parseInt(frete5.value);
            let multQuant5ePrecouni5s = multQuant5ePrecouni5.toFixed(2);

            if (quant5.value == '') {
                multQuant5ePrecouni5s = `...`;
            }
            let result5s = result5.innerHTML = `R$${multQuant5ePrecouni5s} `;
        }, false)

    }, false);

    /* ---------------FIM da function responsavel pela linha 5 ------------------------ */

    /* ----------------------------------------------- */

    quant6.addEventListener('keyup', function () {
        let multQuant6ePrecouni6 = parseInt(quant6.value) * parseInt(precoUni6.value) + parseInt(frete6.value);
        let multQuant6ePrecouni6s = multQuant6ePrecouni6.toFixed(2);

        if (quant6.value == '') {
            multQuant6ePrecouni6s = `...`;
        }
        let result6s = result6.innerHTML = `R$${multQuant6ePrecouni6s} `;

        precoUni6.addEventListener('keyup', function () {
            let multQuant6ePrecouni6 = parseInt(quant6.value) * parseInt(precoUni6.value) + parseInt(frete6.value);
            let multQuant6ePrecouni6s = multQuant6ePrecouni6.toFixed(2);

            if (quant6.value == '') {
                multQuant6ePrecouni6s = `...`;
            }
            let result6s = result6.innerHTML = `R$${multQuant6ePrecouni6s} `;
        }, false)

    }, false);

    /* ---------------FIM da function responsavel pela linha 6 ------------------------ */

    /* ----------------------------------------------- */

    quant7.addEventListener('keyup', function () {
        let multQuant7ePrecouni7 = parseInt(quant7.value) * parseInt(precoUni7.value) + parseInt(frete7.value);
        let multQuant7ePrecouni7s = multQuant7ePrecouni7.toFixed(2);

        if (quant7.value == '') {
            multQuant7ePrecouni7s = `...`;
        }
        let result7s = result7.innerHTML = `R$${multQuant7ePrecouni7s} `;

        precoUni7.addEventListener('keyup', function () {
            let multQuant7ePrecouni7 = parseInt(quant7.value) * parseInt(precoUni7.value) + parseInt(frete7.value);
            let multQuant7ePrecouni7s = multQuant7ePrecouni7.toFixed(2);

            if (quant7.value == '') {
                multQuant7ePrecouni7s = `...`;
            }
            let result7s = result7.innerHTML = `R$${multQuant7ePrecouni7s} `;
        }, false)

    }, false);

    /* ---------------FIM da function responsavel pela linha 7 ------------------------ */

    /* ----------------------------------------------- */

    quant8.addEventListener('keyup', function () {
        let multQuant8ePrecouni8 = parseInt(quant8.value) * parseInt(precoUni8.value) + parseInt(frete8.value);
        let multQuant8ePrecouni8s = multQuant8ePrecouni8.toFixed(2);

        /*  if (quant8.value == '') {
             // multQuant8ePrecouni8s = `...`;
             alert('o numero de quantidade minima é 1');
         } */


        let result8s = result8.innerHTML = `R$${multQuant8ePrecouni8s} `;



        precoUni8.addEventListener('keyup', function () {
            let multQuant8ePrecouni8 = parseInt(precoUni8.value) + parseInt(frete8.value);
            let multQuant8ePrecouni8s = multQuant8ePrecouni8.toFixed(2);

            if (quant8.value == '') {
                multQuant8ePrecouni8s = `...`;
            }
            result8.innerHTML = `R$${multQuant8ePrecouni8s} `;
        }, false)

    }, false);


    quant8.addEventListener('keyup', function () {
        if (quant8.value == '') {
            quant8.value = 0;
            console.log(quant8.value);
        }
        // if (quant8.value != '' || quant8.value != 0) {
        //     quant8.value = quant8.value;
        // }
    }, false)

})(window, document)
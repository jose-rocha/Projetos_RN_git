(function (doc, win) {
    'use strict'
    let Search = doc.querySelector('#Search');


    Search.addEventListener('keyup', function () {
        Search = window.location = "{{id}}";

    });

    setTimeout(Search, 3000);
})(document, window);